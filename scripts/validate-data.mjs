import fs from 'fs';const raw=fs.readFileSync('./data/cultures.json','utf-8');const data=JSON.parse(raw);let ok=true;for(const c of data){for(const k of ['id','name','region','coordinates','summary']){if(c[k]===undefined){console.error(`Missing ${k} in ${c.id||c.name}`);ok=false;}}if(!Array.isArray(c.coordinates)||c.coordinates.length!==2){console.error(`Bad coordinates in ${c.id}`);ok=false;}}console.log(ok?`OK: ${data.length} entries`:'FAILED');process.exit(ok?0:1);