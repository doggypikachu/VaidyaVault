import cultures from '@/data/cultures.json';import Link from 'next/link';export async function generateStaticParams(){const regs=Array.from(new Set(cultures.map(c=>c.region)));return regs.map(r=>({region:r}));}export default function Region({params}){const list=cultures.filter(c=>c.region===params.region);return(<div className='space-y-6'><div className='card'><h1 className='text-2xl font-semibold'>{params.region}</h1><div className='text-white/70'>{list.length} entries</div></div><div className='grid md:grid-cols-3 gap-4'>{list.map(c=>(<Link key={c.id} href={`/archives/${c.id}`} className='p-4 rounded-xl bg-white/5 hover:bg-white/10'><div className='text-lg font-semibold'>{c.name}</div><p className='text-sm text-white/70 line-clamp-3'>{c.summary}</p></Link>))}</div></div>);}